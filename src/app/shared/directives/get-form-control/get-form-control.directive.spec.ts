import { GetFormControlDirective } from './get-form-control.directive';

describe('GetFormControlDirective', () => {
    it('should create an instance', () => {
        const directive = new GetFormControlDirective();
        expect(directive).toBeTruthy();
    });
});
